"""Auto-generated by lib/schema-generator/langchain-package-generator.ts from
public/schemas/pba-tenders-monitor/openapi.json — do not hand-edit. Regenerate instead.
"""
from typing import Optional

from pydantic import BaseModel, Field
from langchain_core.tools import StructuredTool

from ._client import call_apify_actor

APIFY_ACTOR_ID = 'yWvRQyWSyGVLJPtQ7'  # stefano_seggio/pba-tenders-monitor — immutable Apify ID,
# not the slug: the OpenAPI adapter this package's data is sourced from was itself corrected this
# session to call by ID rather than slug, after a real slug-rename incident (primer-actor ->
# page-metadata-extractor) — kept consistent here for the same reason.


class PbaTendersMonitorInput(BaseModel):
    """Mirrors pba-tenders-monitor's real .actor/input_schema.json field-for-field."""

    views: Optional[list[str]] = Field(default=["apertura_proxima","ultimos_30_dias","adjudicados"], min_length=1, description='Which PBAC grids to extract. Defaults to all three.')
    fetchFullDetail: Optional[bool] = Field(default=False, description='When enabled, simulates the postback for each process. Known limitation: this currently returns only a short status label, not the full Pliego document text - that content lives in a nested panel not yet mapped. Leave off until this is resolved; it adds cost with limited extra value today.')
    maxItems: Optional[int] = Field(default=200, ge=1, description='Hard cap on the number of tender processes returned this run, across all selected views.')
    onlyNew: Optional[bool] = Field(default=False, description='For recurring/scheduled runs: return only tenders that are new, changed vistaOrigen/estado, amended or closed since a prior run of this actor (tracked in a named key-value store scoped to this actor). CLOSED (a tender no longer in any of the 3 grids) is only ever computed when \'Views to extract\' covers all 3 default views - a narrower selection makes this run\'s fetch a subset of the site, so CLOSED is skipped (and logged) in that case.')
    eventTypes: Optional[list[str]] = Field(default=["NEW_LISTING","STATUS_CHANGE","UPDATED","CLOSED"], description='Which kinds of change to deliver when \'Only new records\' is on (ignored, everything delivered, when it is off). NEW_LISTING = never seen before. STATUS_CHANGE = vistaOrigen or estado changed - e.g. a tender moving from upcoming to awarded. UPDATED = same vistaOrigen/estado, a field changed. CLOSED = no longer listed in any of the 3 grids (only computed on an unfiltered \'Views to extract\').')
    dateRange: Optional[str] = Field(default=None, description='Restricts results to tenders whose fechaApertura falls within this window ending now. Independent of onlyNew. Note: fechaApertura is WHEN BIDS OPEN, not when the tender was published - for the \'Upcoming openings\' view specifically that date is routinely in the future, so this filter will not match those rows (see AGENTS.md/README).')


def _pba_tenders_monitor(views: Optional[list[str]] = None, fetchFullDetail: Optional[bool] = None, maxItems: Optional[int] = None, onlyNew: Optional[bool] = None, eventTypes: Optional[list[str]] = None, dateRange: Optional[str] = None) -> list[dict]:
    payload = {
        **({"views": views} if views is not None else {}),
        **({"fetchFullDetail": fetchFullDetail} if fetchFullDetail is not None else {}),
        **({"maxItems": maxItems} if maxItems is not None else {}),
        **({"onlyNew": onlyNew} if onlyNew is not None else {}),
        **({"eventTypes": eventTypes} if eventTypes is not None else {}),
        **({"dateRange": dateRange} if dateRange is not None else {}),
    }
    return call_apify_actor(APIFY_ACTOR_ID, payload)


pba_tenders_monitor_tool = StructuredTool.from_function(
    func=_pba_tenders_monitor,
    name='pba-tenders-monitor',
    description='PBAC is the official public tender portal for Buenos Aires Province, Argentina, not the city of Buenos Aires (CABA) or national procurement. Extracts upcoming openings, last-30-day tenders, and awarded processes, with delta mode flagging new listings, status changes, amendments, and closures.',
    args_schema=PbaTendersMonitorInput,
)
