"""Auto-generated by delta-registry-website/lib/schema-generator — do not hand-edit.
Source: Apify actor 'aozora-bunko-public-domain-text-feed' (live Apify API), generated 2026-09-18.
Regenerate with: node generate-all.js

NOTE: this targets AG2 Classic (import autogen), the AutoGen-lineage conversational-agent API.
"AutoGen" itself is in Microsoft-maintenance mode as of 2026; AG2 (ag2ai/ag2) is the actively
developed continuation. AG2 also has a newer, separate v1.0 protocol-driven surface (shipped
2026-09-11) not covered here because its docs could not be verified at generation time.
"""
import os
from typing import Annotated, Optional

import requests
from autogen import ConversableAgent, register_function, LLMConfig

APIFY_ACTOR_ID = 'K0XRDbUteacQL3jeF'  # stefano_seggio/aozora-bunko-public-domain-text-feed
APIFY_RUN_SYNC_URL = f"https://api.apify.com/v2/acts/{APIFY_ACTOR_ID}/run-sync-get-dataset-items"


def run_aozora_bunko_public_domain_text_feed(
    onlyNew: Annotated[Optional[bool], 'When true (default), only NEW_TEXT, REVISED_TEXT and STATUS_CHANGE rows are pushed, each as a charged PPE event. Set to false for a free baseline run: every catalog row that passes the public-domain filter is pushed once as an uncharged SNAPSHOT_NO_DIFF record.'] = true,
    resetState: Annotated[Optional[bool], 'When true, clears this feed\'s stored seen-work state before this run, so the next run re-baselines from scratch (every catalog row becomes a fresh, uncharged SNAPSHOT_NO_DIFF again, subject to onlyNew). Use for testing or recovering a corrupted state store, not routine operation.'] = false,
    onlyConfirmedPublicDomain: Annotated[Optional[bool], 'When true (default), keeps only catalog rows where Aozora\'s own work-copyright flag reads なし (no copyright - confirmed public domain). When false, also includes rows flagged あり (the author/estate granted free-use permission, but the work is NOT public domain).'] = true,
    includeFullText: Annotated[Optional[bool], 'When true (default), NEW_TEXT/REVISED_TEXT events download the linked text ZIP, decode it from Shift_JIS, strip Aozora\'s ruby/annotation markup and legend preamble, and embed both plain_text and raw_text_with_markup - delivered as the higher-priced \'result\' event. When false, or when a text file fails to download/decode, the same events are delivered as metadata-only \'result-summary\' records instead.'] = false,
    authorIdAllowlist: Annotated[Optional[list[str]], 'Aozora 人物ID values (e.g. "001257") to restrict this feed to. Leave empty to monitor the entire catalog.'] = [],
    maxItemsPerRun: Annotated[Optional[int], 'Caps the number of charged (result/result-summary) events this run will push, independent of your Apify spending limit. 0 means no Actor-side cap.'] = 20,
    requestDelayMs: Annotated[Optional[int], 'Aozora Bunko is a small, volunteer-run, non-commercial mirror with no published rate limit - this default trades a slower walk for not needing to trust an unconfirmed number, and for being a polite citizen of a small server, not only a cost control.'] = 500,
    maxRetries: Annotated[Optional[int], 'Retry attempts (exponential backoff with jitter) for a 429/5xx response or a network-level failure before a single catalog or text-ZIP fetch is given up on as failed.'] = 5,
    requestTimeoutSecs: Annotated[Optional[int], 'Aborts a single request to Aozora Bunko (catalog export or per-work text ZIP) that takes longer than this, so one slow response never stalls the whole run.'] = 30,
    deltaStateName: Annotated[Optional[str], 'Names the Key-Value Store this feed\'s seen-work state is kept in. Run multiple independent schedules - e.g. filtered to different author allowlists - without them draining each other\'s baseline by giving each a distinct name here.'] = "default",
) -> str:
    """Watches Aozora Bunko's own official catalog export for newly digitized or revised confirmed-public-domain Japanese texts and emits NEW_TEXT/REVISED_TEXT/STATUS_CHANGE events with full decoded text where available. Pay-per-event: billed only for what's new or changed."""
    token = os.environ["APIFY_TOKEN"]
    payload = {
        **({"onlyNew": onlyNew} if onlyNew is not None else {}),
        **({"resetState": resetState} if resetState is not None else {}),
        **({"onlyConfirmedPublicDomain": onlyConfirmedPublicDomain} if onlyConfirmedPublicDomain is not None else {}),
        **({"includeFullText": includeFullText} if includeFullText is not None else {}),
        **({"authorIdAllowlist": authorIdAllowlist} if authorIdAllowlist is not None else {}),
        **({"maxItemsPerRun": maxItemsPerRun} if maxItemsPerRun is not None else {}),
        **({"requestDelayMs": requestDelayMs} if requestDelayMs is not None else {}),
        **({"maxRetries": maxRetries} if maxRetries is not None else {}),
        **({"requestTimeoutSecs": requestTimeoutSecs} if requestTimeoutSecs is not None else {}),
        **({"deltaStateName": deltaStateName} if deltaStateName is not None else {}),
    }
    resp = requests.post(
        APIFY_RUN_SYNC_URL,
        params={"token": token},
        json=payload,
        timeout=320,  # Apify's real run-sync-get-dataset-items cap is 300s
    )
    resp.raise_for_status()
    return resp.text


# Wire into your own agents, e.g.:
#
# with LLMConfig(api_type="openai", model="gpt-4o-mini"):
#     compliance_agent = ConversableAgent(name="compliance_agent", system_message="...")
# executor_agent = ConversableAgent(name="executor_agent", human_input_mode="NEVER")
#
# register_function(
#     run_aozora_bunko_public_domain_text_feed,
#     caller=compliance_agent,
#     executor=executor_agent,
#     description='Watches Aozora Bunko\'s own official catalog export for newly digitized or revised confirmed-public-domain Japanese texts and emits NEW_TEXT/REVISED_TEXT/STATUS_CHANGE events with full decoded text where available. Pay-per-event: billed only for what\'s new or changed.',
# )
