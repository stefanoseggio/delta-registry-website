// Auto-generated by delta-registry-website/lib/schema-generator — do not hand-edit.
// Source: Apify actor 'entrerios-compras-monitor' (local .actor/input_schema.json), generated 2026-09-18.
// Regenerate with: node generate-all.js
import { tool } from "langchain";
import * as z from "zod";

const APIFY_ACTOR_ID = "oiXeFzZGlIQ6mKgoo"; // stefano_seggio/entrerios-compras-monitor

export const entreriosComprasMonitorTool = tool(
  async (input) => {
    const token = process.env.APIFY_TOKEN;
    if (!token) throw new Error("APIFY_TOKEN is not set");
    const res = await fetch(
      `https://api.apify.com/v2/acts/${APIFY_ACTOR_ID}/run-sync-get-dataset-items?token=${token}`,
      { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(input) },
    );
    if (!res.ok) throw new Error(`Apify run failed: ${res.status} ${res.statusText}`);
    return res.json(); // dataset items array, returned directly
  },
  {
    name: "entrerios-compras-monitor",
    description: "Extracts the full public tender backlog of the Province of Entre Rios, Argentina's Unidad Central de Contrataciones - 5,505 records verified live, back to at least 2004 - with organism, objeto, and status detail, and flags new, status-changed, and closed tenders across scheduled runs.",
    schema: z.object({
    estado: z.enum(["", "1", "2", "3", "4"]).describe("Filter by process status. Leave blank (default) to pull every status in one request - the site's own union of all 4, verified live to be far larger than any single status alone.").default(""),
    tipoLicitacion: z.enum(["", "1", "2", "3", "4"]).describe("Filter by contracting procedure type. Leave blank for all types.").default(""),
    organismo: z.enum(["", "1", "3", "4", "5", "6", "7", "8", "9"]).describe("Filter by issuing organism. Leave blank for all organisms.").default(""),
    anio: z.string().describe("Filter by year, e.g. \"2025\". Leave blank for all years (data goes back to at least 2004).").default(""),
    palabra: z.string().describe("Free-text keyword filter, matched server-side the same way the site's own \"Palabra\" search box works.").default(""),
    maxItems: z.number().int().min(1).describe("Hard cap on the number of tenders returned this run. The full unfiltered backlog is over 5000 rows (verified live) - narrow with the filters above or raise this cap to pull all of it.").default(1000),
    onlyNew: z.boolean().describe("For recurring/scheduled runs: return only tenders that are new, changed estado, or closed since a prior run of this actor (tracked in a named key-value store scoped to this actor, so it survives between scheduled runs). This source is fetched as a single unfiltered POST covering the entire backlog rather than genuine, reliably newest-first pagination (verified live - see AGENTS.md), so enabling this does NOT reduce fetch time or request volume: the full backlog is still fetched every run either way, and this only changes which records get pushed to the dataset afterward.").default(false),
    eventTypes: z.array(z.string()).describe("Which kinds of change to deliver when 'Only new records' is on (ignored, everything delivered, when it is off). NEW_LISTING = never seen before. STATUS_CHANGE = seen before, estado changed - free to detect, record_id's own hash excludes estado on purpose. CLOSED = a previously-seen record_id is absent from this run's fetch - only computed on an UNFILTERED run (no status/type/organism/year/keyword filter set), since a filtered run's fetch is a subset of the register, not the whole thing; skipped otherwise.").default(["NEW_LISTING","STATUS_CHANGE","CLOSED"]),
    dateRange: z.enum(["", "24h", "7d", "30d"]).describe("Filters to a source's own natural per-record date field, where one of this portfolio's monitor actors has one. This source (Entre Rios' tender listing) publishes NO per-record date field anywhere - not on the listing, not on any detail page (there isn't one) - verified live against the real page (see AGENTS.md). Setting this has NO effect on the results returned; the actor logs a warning instead of silently applying a misleading filter. Present only for input-shape consistency with the rest of the portfolio.").default(""),
    }),
  },
);
